interface:
  display_name: "Doppler"
  short_description: "Critical workflow for any secrets, tokens, API keys, and Doppler secret injection"
  default_prompt: "Use $doppler before handling any secrets, tokens, API keys, .env files, CI secrets, or Doppler secret injection."
