interface:
  display_name: "Doppler"
  short_description: "Handle secrets safely with Doppler-backed injection"
  default_prompt: "CRITICAL: Use $doppler before working with secrets, tokens, API keys, .env files, CI credentials, or Doppler secret injection."
