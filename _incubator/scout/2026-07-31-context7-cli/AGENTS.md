# Scout Guard: danilo-aguiar-br/context7-cli Snapshot

`source/` is a verbatim capture of an external repository, kept as evidence.

It contains no agent-instruction files (no `CLAUDE.md`, `AGENTS.md`, `.cursorrules`, or
copilot instructions), but its `README.md`, `CONTRIBUTING.md`, and doc comments still
describe another project's conventions. None of that applies here. Authority for this
repository is the root `AGENTS.md`.

`source/` is read-only. Do not edit, rename, or delete anything in it.
