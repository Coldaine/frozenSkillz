Adapted ideas only. Never raw source dumps.

- `delegation-handoff-contract.md` — the *send*-side spec the candidate never wrote, mirroring its
  return contract. Seven fields a spawn prompt must carry, derived during intake and refined
  against published prior art.

Incubator-only. Nothing here is active content, and nothing may be copied into `plugins/` or
`docs/` until the promotion gate in `../decisions.md` is discharged.
